
let cities = new Array(5); //array constructor method 
let cities2 = new Array (5,10) 
let canadianCities = new Array('Toronto", hamilton', 'ajax');

let countries = Array.of('canada', 'india', 'russia', 'america');

let chars = Array.from ('humber');

console.log (cities); 
console.log (cities2);
console.log (canadianCities);
console.log (countries)
console.log (chars)


let students = ['bob', 'tim', 'amy', 'sam']
console.log (students[0])
console.log (students[1]) //[only reading 1st person with square braket]] 
students[0] = 'Paul';
students[4] = 'tim'; //adding new person with [] 

students.push ('jon') //adds person at the end// with any method you use you can use push through all 
console.log (students) 

console.log (students.pop())// no arguement removes last element only from the end 
console.log (students);
students.unshift('matt'); //shift removes unshift adds
console.log (students);

let selectedStudents = students.slice(1, 3)
console.log (selectedStudents);
console.log (students);

students.splice(2, 2); //splice() in JavaScript is an array method that adds, removes, or replaces elements in an array by modifying the original array.
console.log (students); 
                                            // first number is where you want to add your array
students.splice (1,0, 'john', "eva","ava"); //second number is things you want to delete 
console.log (students); 


// Lab 2: 
//Part A
Start
    declare string foodname
    declare real  fatGrams
    declare real  numOfServings
    declare real  totalFatCalories = fatGrams * cals_per_fat_grams
    declare real  fatCaloriesPerServing

    const caloriesPerGram = 9

display "$(foodname) has $(fatCaloriesPerServing) fat calories fatCaloriesPerServing

    display "Enter the food name:"
    input foodname

    display "enterfatgrams:"
    fatGrams = 275
    //input fatGrams for user to type it but we know its 275 from lab instruction

    display "numofservings:"
    input numOfServings
    totalFatCalories = fatGrams * caloriesPerGram
    fatCaloriesPerServing = totalFatCalories / numOfServings

    output "Total fat calories in ", foodname, ": ", totalFatCalories
    output "Fat calories per serving in ", foodname, ": ", fatCaloriesPerServing


End


//Part B

Start

    declare string foodname //fish
    declare real  fatGrams //206
    declare real  numOfServings //3
    declare real  totalFatCalories //output
    declare real  fatCaloriesPerServing //total output


    const caloriesPerGram = 9

    display "enterfoodname:"
    input foodname

    display "enterfatgrams:" //fish 
    fatGrams = 206
    //"enter fat grams here" if we didnt know already 

    display "numofservings:"
    input numOfServings
    
    totalFatCalories = fatGrams * caloriesPerGram
    fatCaloriesPerServing = totalFatCalories / numOfServings

    output "Total fat calories in ", foodname, ": ", totalFatCalories
    output "Fat calories per serving in ", foodname, ": ", fatCaloriesPerServing

End 
